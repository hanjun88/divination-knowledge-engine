# Google Drive Reference Export

This is a non-executable reference export for `hanjun88/divination-knowledge-engine`. GitHub runtime code remains authoritative. Only explicitly mapped Drive assets are included; unrelated Drive material is excluded. See `manifest.json`.
